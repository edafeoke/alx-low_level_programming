#include "holberton.h"

/**
 * puts2 - Prints string in even intervals
 * @s: the string to print
 *
 * Return: void
 */
void puts2(char *s)
{
	int i = 0;

	while (s[i])
	{
		if
		_putchar(s[i]);
		i++;
	}
	_putchar('\n');
}

