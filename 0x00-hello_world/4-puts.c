#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return:  Always 0 (Success)
 */
int main(void)
{
	char string[52];

	strcpy(string, "\"Programming is like building a multilingual puzzle\n");
	puts(string);
	return (0);
}

